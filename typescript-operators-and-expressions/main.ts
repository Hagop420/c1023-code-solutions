// 1st problem with width/height

const width: number = 30;

const height: number = 20;

const area: number = width * height;

console.log('Value of area:', area);
console.log('typeof area:', typeof area);

// 2nd problem

const bill: number = 800;
const payment: number = 344;

const change: number = bill - payment;

console.log('Value of change:', change);
console.log('typeof change:', typeof change);

// 3rd problem

const quizzes: number = 100;
const midterm: number = 70;
const final: number = 50;

// 4th solution

const grade: number = (quizzes * midterm * final) / 3;

console.log('value of grade:', grade);
console.log('typeof grade:', typeof grade);

// 5th solution

const firstName: string = 'Jack';

const lastName: string = 'Almadjian';

const fullName: string = `${firstName} ${lastName}`;

console.log(`My full name is ${fullName}`);

console.log(`typeof fullName is a: ${typeof fullName}`);

// 6th solution

const pH: number = 5;

const isAcidic: boolean = pH < 7;

console.log(`Value of isAcidic ${isAcidic}`);

console.log(`typeof isAcidic ${typeof isAcidic}`);

// 7th solution
const headCount: number = 30;

const isSparta: boolean = headCount === 300;

console.log(`Value of isSparta ${isSparta}`);

console.log(`typeof isSparta ${typeof isSparta}`);

// final solution
const motto: string = fullName + ' is the 🐐';

console.log(`Value of motto ${motto}`);

console.log(`typeof motto ${typeof motto}`);
